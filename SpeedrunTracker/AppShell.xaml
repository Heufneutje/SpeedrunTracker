<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:ffil="clr-namespace:FFImageLoading.Maui;assembly=FFImageLoading.Maui"
       xmlns:loc="clr-namespace:SpeedrunTracker.Localization"
       xmlns:views="clr-namespace:SpeedrunTracker.Views"
       xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
       x:Class="SpeedrunTracker.AppShell"
       FlyoutBackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Gray900}}">
    <Shell.ItemTemplate>
        <DataTemplate x:DataType="BaseShellItem">
            <Grid Style="{StaticResource FlyoutItemStyle}"
                  ColumnDefinitions="50,150">
                <ffil:CachedImage Source="{Binding FlyoutIcon}"
                                  Margin="10"
                                  HeightRequest="30">
                    <ffil:CachedImage.Behaviors>
                        <toolkit:IconTintColorBehavior TintColor="{StaticResource White}" />
                    </ffil:CachedImage.Behaviors>
                </ffil:CachedImage>
                <Label Grid.Column="1"
                       Text="{Binding Title}"
                       Style="{StaticResource FlyoutItemLabelStyle}" />
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <Shell.FlyoutHeader>
        <VerticalStackLayout Style="{StaticResource FlyoutHeaderStyle}">
            <Label Text="{loc:Translate AppName}"
                   FontSize="Title"
                   Padding="20"
                   TextColor="{StaticResource White}" />
            <ffil:CachedImage Source="main"
                              HeightRequest="150"
                              HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Shell.FlyoutHeader>

    <FlyoutItem FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent ContentTemplate="{DataTemplate views:FollowingPage}"
                      Route="Following"
                      Title="{loc:Translate MenuItemFavoritesTitle}"
                      Icon="following" />
        <Tab Title="{loc:Translate MenuItemSearchTitle}"
             Icon="search">
            <ShellContent ContentTemplate="{DataTemplate views:GameSearchPage}"
                          Route="SearchGames"
                          Title="{loc:Translate MenuItemSearchGamesTabTitle}" />
            <ShellContent ContentTemplate="{DataTemplate views:GameSeriesSearchPage}"
                          Route="SearchSeries"
                          Title="{loc:Translate MenuItemSearchGameSeriesTabTitle}" />
            <ShellContent ContentTemplate="{DataTemplate views:UserSearchPage}"
                          Route="SearchUsers"
                          Title="{loc:Translate MenuItemSearchUsersTabTitle}" />
        </Tab>
        <ShellContent ContentTemplate="{DataTemplate views:NotificationsPage}"
                      Route="Notifications"
                      Title="{loc:Translate MenuItemNotificationsTitle}"
                      Icon="bell" />
    </FlyoutItem>
    <FlyoutItem Title="{loc:Translate MenuItemProfileTitle}"
                FlyoutIcon="user">
        <ShellContent ContentTemplate="{DataTemplate views:ProfilePage}"
                      Route="Profile" />
    </FlyoutItem>

    <FlyoutItem Title="{loc:Translate MenuItemSettingsTitle}"
                FlyoutIcon="gear">
        <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}"
                      Route="Settings" />
    </FlyoutItem>

    <FlyoutItem Title="{loc:Translate MenuItemAboutTitle}"
                FlyoutIcon="about">
        <ShellContent ContentTemplate="{DataTemplate views:AboutPage}"
                      Route="about" />
    </FlyoutItem>
</Shell>
