<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SpeedrunTracker.Views"
             xmlns:viewmodels="clr-namespace:SpeedrunTracker.ViewModels"
             x:DataType="viewmodels:SearchEntityViewModel"
             x:Class="SpeedrunTracker.Views.SearchPage"
             Title="Search">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <SearchBar x:Name="searchBar"
                   Placeholder="Search..."
                   SearchCommand="{Binding SearchCommand}"
                   SearchCommandParameter="{Binding Text, Source={x:Reference searchBar}}"
                   Grid.Row="0"
                   HorizontalOptions="FillAndExpand"
                   SearchButtonPressed="searchBar_SearchButtonPressed"></SearchBar>
        <local:SpeedrunEntitiesView Grid.Row="1" />
    </Grid>
</ContentPage>
