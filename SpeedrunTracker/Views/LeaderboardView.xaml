<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SpeedrunTracker.Views.LeaderboardView">
    <ListView ItemsSource="{Binding Leaderboard.Runs}" VerticalOptions="FillAndExpand" VerticalScrollBarVisibility="Never" CachingStrategy="RecycleElementAndDataTemplate" SelectionMode="None" HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid Padding="5,20,20,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Label Text="{Binding Place}" Grid.Column="0" HorizontalTextAlignment="Center"/>
                        <Label Text="{Binding Run.Players[0].DisplayName}" TextColor="{Binding Run.Players[0].DisplayNameStyle.ColorFrom.DarkColor}" FontAttributes="Bold" Grid.Column="1"/>
                        <Label Text="{Binding Run.Times.PrimaryTimeSpan}" Grid.Column="2"/>
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentView>
